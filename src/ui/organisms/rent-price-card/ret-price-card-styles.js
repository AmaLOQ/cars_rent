import { Paper, styled, Box, Typography } from "@mui/material";

export const StyledPaperRentPriceCard = styled()`
	width: 370px;
	height: 390px;
	padding: 2px;
	background: ${props => props.theme.palette.white};
	box-shadow: 0px 4px 18px rgba(0, 0, 0, 0.15);
	border-radius: 4px;
`;
